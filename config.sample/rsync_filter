# includes
+ /var/
+ /var/di/
+ /var/view_preprocessed/
+ /var/resource_config.json

# excludes
# media is copied separately (without --delete)
- /var/**
- /pub/media
- /.git/
- /app/etc/env.php
- /dev/
- /sitemap.xml
